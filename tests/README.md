# Test Coverage Report

## Current Test Status

Tests will be automatically run on every push and pull request via GitHub Actions.

Run locally:
```bash
pytest tests/ -v --cov=src
```

## Sample Tests

See `tests/` directory for examples of:
- Data preprocessing tests
- Model training and prediction tests
- Evaluation metrics tests
- Configuration loading tests
